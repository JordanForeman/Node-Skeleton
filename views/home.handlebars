<h1>Example App: Home</h1>

{{#if user }}
	<h1>Welcome Home{{#if user.name}} {{ user.name }}{{/if}}!</h1>
	<a href="logout">Logout</a>
{{else }}
	<a href="login">Login</a>
	or
	<a href="register">Register</a>
{{/if}}